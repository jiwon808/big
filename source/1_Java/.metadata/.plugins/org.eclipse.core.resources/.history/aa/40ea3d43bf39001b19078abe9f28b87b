package com.lec.ex01_store;
//추상(abstract)클래스; 추상메소드가 1개 이상 있는 클래스 
//추상클래스가지고 new만들일 없어 
public abstract class HeadQuarterStore {
	private String name;
	public HeadQuarterStore(String name) {
		this.name=name; //this==the current object of a method or a constructor. 
						//remove the confusion between class attributes and parameters that have same names.
	}
	public abstract void kimchi(); //추상 함수; 함수 선언만함   원래 메소드 선언,구헌 다 함.  
	public abstract void bude();
	public abstract void bibib();
	public abstract void sunde(); //이거 필요없어서 반드시 override하세요 
	public abstract void bab() ;
	public String getName() {
		return name;
	}
}
